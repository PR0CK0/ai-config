# TODO

- [ ] Add a domain-specific section to `AGENTS.md` covering ontology-grounded LLM development philosophy — how to approach ontology work, when to reach for RDF vs property graphs vs schemas, first-class treatment of knowledge graphs. Currently this lives implicitly in the `ontologist` agent but should be surfaced at the top level.
