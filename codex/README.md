# Codex CLI Config

`AGENTS.md` here is generated from root `AGENTS.md` via `build.sh`. Do not edit it directly.

```sh
cp AGENTS.md ~/AGENTS.md          # global
# or copy to project root for per-project scope
```
